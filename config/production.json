{
  "host": "HOST",
  "port": "PORT",
  "authentication": {
    "secret": "AUTH_SECRET",
    "auth0": {
      "clientID": "AUTH0_CLIENT_ID",
      "clientSecret": "AUTH0_CLIENT_SECRET"
    }
  },
  "services": {
    "acl": {
      "persistence": {
        "mongodb": {
          "prefix": "acl_",
          "url": "MONGOHQ_URL",
          "replicaSet": "MONGO_REPLICA_SET"
        }
      }
    },
    "resources": {
      "persistence": {
        "mongodb": {
          "prefix": "res_",
          "url": "MONGOHQ_URL",
          "replicaSet": "MONGO_REPLICA_SET"
        }
      }
    },
    "system": {
      "persistence": {
        "mongodb": {
          "prefix": "sys_",
          "url": "MONGOHQ_URL",
          "replicaSet": "MONGO_REPLICA_SET"
        }
      }
    }
  }
}
