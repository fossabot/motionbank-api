{
  "host": "HOST",
  "port": "PORT",

  "services": {
    "acl": {
      "persistence": {
        "mongodb": {
          "prefix": "acl_",
          "url": "MONGODB_URI",
          "dbName": "MONGO_DBNAME"
        }
      }
    },
    "resources": {
      "persistence": {
        "mongodb": {
          "prefix": "res_",
          "url": "MONGODB_URI",
          "dbName": "MONGO_DBNAME"
        }
      }
    },
    "system": {
      "persistence": {
        "mongodb": {
          "prefix": "sys_",
          "url": "MONGODB_URI",
          "dbName": "MONGO_DBNAME"
        }
      }
    }
  }
}
