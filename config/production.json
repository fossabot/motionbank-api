{
  "host": "HOST",
  "port": "PORT",
  "authentication": {
    "secret": "AUTH_SECRET",
    "auth0": {
      "clientID": "AUTH0_CLIENT_ID",
      "clientSecret": "AUTH0_CLIENT_SECRET"
    }
  },
  "services": {
    "acl": {
      "persistence": {
        "mongodb": {
          "prefix": "acl_",
          "url": "MONGODB_URI",
          "dbName": "MONGO_DBNAME"
        }
      }
    },
    "resources": {
      "persistence": {
        "mongodb": {
          "prefix": "res_",
          "url": "MONGODB_URI",
          "dbName": "MONGO_DBNAME"
        }
      }
    },
    "system": {
      "persistence": {
        "mongodb": {
          "prefix": "sys_",
          "url": "MONGODB_URI",
          "dbName": "MONGO_DBNAME"
        }
      }
    }
  }
}
